## k8s-deploy validate

Validate Kubernetes manifests against schemas

### Synopsis

Validate one or more YAML files or directories containing Kubernetes manifests.
If no arguments are provided, manifests are read from stdin.

```
k8s-deploy validate [files or directories] [flags]
```

### Options

```
  -h, --help                        help for validate
      --ignore-missing-schemas      Skip validation for resource kinds without a schema
      --kubernetes-version string   Kubernetes version to validate against (default "1.29.0")
      --strict                      Disallow additional properties not in the schema (default true)
```

### Options inherited from parent commands

```
  -v, --verbose   Enable verbose (debug) logging
```

### SEE ALSO

* [k8s-deploy](k8s-deploy.md)	 - A CLI tool for validating Kubernetes manifests, automating Ingress with TLS, and syncing DNS records.

###### Auto generated by spf13/cobra on 18-Jan-2026

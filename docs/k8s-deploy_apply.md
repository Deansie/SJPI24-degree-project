## k8s-deploy apply

Apply Kubernetes manifests to the cluster

### Synopsis

Applies validated YAML manifests to the Kubernetes cluster using client-go.
Supports kubeconfig for authentication and namespace/context overrides.

Examples:
  k8s-deploy apply --file manifest.yaml --namespace myapp
  k8s-deploy apply --dir manifests/ --dry-run

```
k8s-deploy apply [flags]
```

### Options

```
      --dir string          Directory containing YAML manifests
      --dry-run             Simulate apply without changes
      --file string         Path to YAML manifest file
  -h, --help                help for apply
      --kubeconfig string   Path to kubeconfig (default "/home/dennis-andersen/.kube/config")
      --namespace string    Namespace to apply to (overrides manifest)
```

### Options inherited from parent commands

```
  -v, --verbose   Enable verbose (debug) logging
```

### SEE ALSO

* [k8s-deploy](k8s-deploy.md)	 - A CLI tool for validating Kubernetes manifests, automating Ingress with TLS, and syncing DNS records.

###### Auto generated by spf13/cobra on 18-Jan-2026

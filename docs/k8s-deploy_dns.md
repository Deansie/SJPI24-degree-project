## k8s-deploy dns

Sync or manage DNS records in Cloudflare for Kubernetes services/Ingress

### Synopsis

Idempotently creates, updates, or checks DNS records (e.g., A/CNAME) in Cloudflare to point to your Ingress
load balancer. Handles cases where records already exist (update) or need creation.

Used in deployment flows to ensure domains resolve correctly before applying manifests.

Examples:
  k8s-deploy dns sync --domain app.example.com --target 192.0.2.1
  k8s-deploy dns list --zone example.com

### Options

```
  -h, --help   help for dns
```

### Options inherited from parent commands

```
  -v, --verbose   Enable verbose (debug) logging
```

### SEE ALSO

* [k8s-deploy](k8s-deploy.md)	 - A CLI tool for validating Kubernetes manifests, automating Ingress with TLS, and syncing DNS records.
* [k8s-deploy dns list](k8s-deploy_dns_list.md)	 - List DNS records in a Cloudflare zone
* [k8s-deploy dns sync](k8s-deploy_dns_sync.md)	 - Idempotently create or update DNS records in Cloudflare

###### Auto generated by spf13/cobra on 18-Jan-2026

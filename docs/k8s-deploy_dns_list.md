## k8s-deploy dns list

List DNS records in a Cloudflare zone

### Synopsis

List DNS records for a Cloudflare zone.

This command is read-only and safe for CI/CD pipelines.

```
k8s-deploy dns list [flags]
```

### Options

```
  -h, --help          help for list
      --type string   Filter by record type (A, CNAME, MX, TXT)
      --zone string   DNS zone (e.g. example.com)
```

### Options inherited from parent commands

```
  -v, --verbose   Enable verbose (debug) logging
```

### SEE ALSO

* [k8s-deploy dns](k8s-deploy_dns.md)	 - Sync or manage DNS records in Cloudflare for Kubernetes services/Ingress

###### Auto generated by spf13/cobra on 18-Jan-2026
